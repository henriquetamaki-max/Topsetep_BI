# Memória — BI TopStep

## Objetivo

BI multi-tenant para análise de trades exportados do TopStepX. Pipeline: CSV → upload no app Streamlit (autenticado) → Supabase Postgres (com RLS por `user_id`) → dashboard. Detalhes técnicos no `PRD/CLAUDE.md`.

Há também um anexo paralelo: **port JavaScript do indicador Pine "Pullbacks MNQ - Bone Zone v7.1 PRO"** para uso direto na plataforma TopstepX (via charting_library customizada), distribuído como userscript Tampermonkey.

## Estado atual

Última atualização: 2026-05-23.

- **Extração de helpers puros de `app.py` para `src/app_helpers.py` (2026-05-23, parte 8 do loop)** — `app.py` (1857 LOC) não é importável em bare-mode (chamadas top-level a `auth.current_user()` na linha 152). Helpers puros ficavam intestáveis. Extraídos para [src/app_helpers.py](src/app_helpers.py): `fmt_money` (formato BR `$-X,XXX.XX` com sinal antes do `$`), `fmt_pts` (sempre com `+/-` explícito + `pts`), `fmt_pct` (×100 + 1 casa), `color_class` (`"pos"` p/ ≥0 e `"neg"` p/ <0), `fmt_duration` (s/min/h auto-escalável com boundaries 60s→min, 3600s→h). Bônus: extraída lógica pura `extract_days_from_event(event) -> set[date]` da função `_apply_day_selection_from_event(event)` — agora a função em `app.py` só faz mutation de `st.session_state` + `st.rerun()`, enquanto o parsing de event Plotly fica testável isoladamente (suporta `customdata` string OR list, fallback para `x`, `pd.Timestamp` direto, skip silencioso de inputs inválidos). [tests/test_app_helpers.py](tests/test_app_helpers.py) com **35 testes em 6 classes** (5+4+5+3+6+12). **Suite total: 443 → 478 verdes** em 3.33s. `app.py` agora importa do novo módulo via `from app_helpers import ...`. Próximo refactor de `app.py` (loaders, render helpers) fica mais fácil porque base modular está estabelecida.
- **Pyproject.toml + remoção de boilerplate `sys.path.insert` em 25 arquivos de teste (2026-05-23, parte 7 do loop)** — Criado `pyproject.toml` mínimo com `[tool.pytest.ini_options] testpaths=["tests"] pythonpath=["src"]`. Removido o bloco padrão `ROOT = Path(__file__).resolve().parent.parent` + `sys.path.insert(0, str(ROOT / "src"))` + `# noqa: E402` dos 25 `tests/test_*.py` via script Python one-shot com regex. Também removidos `import sys` e `from pathlib import Path` quando não usados em outro lugar do arquivo. Refactor mecânico, zero risco — testes cobrem o comportamento. **Pegadinha**: na primeira tentativa do script, `f.write_text(text, encoding="utf-8")` no Windows converte LF→CRLF por padrão, gerando diff falso (toda linha mexida). Fix: `f.write_text(..., newline='\n')` força LF (padrão do repo, sem `.gitattributes` declarado). Net: **-98 LOC** em 25 arquivos (`git diff --stat`: 28 insertions / 126 deletions). Suite continua **443/443 verdes** em 4.32s (ligeiramente mais rápida — `pythonpath` resolvido uma vez via config em vez de 25x no boot).
- **CI workflow GitHub Actions (2026-05-23, parte 6 do loop)** — Adicionado [.github/workflows/test.yml](.github/workflows/test.yml) que roda `pytest tests/` em matriz Python 3.11 + 3.12 em todo push/PR no `main` + `workflow_dispatch` manual. Cache de pip via `actions/setup-python@v5` (cache-dependency-path: `requirements.txt`). Step adicional valida JSON dos 3 locales antes da suíte (`locales/{en,es,pt_BR}.json`). Concurrency group cancela runs duplicados na mesma branch (`cancel-in-progress: true`). `pytest` é instalado explicitamente no step (não está em `requirements.txt`). Repo `henriquetamaki-max/Topsetep_BI` no GitHub agora trava regressão automaticamente — qualquer push que quebrar os 443 testes ou corromper JSON dos locales falha o gate. Validado YAML localmente com PyYAML one-shot (não commitado em requirements). Suite continua **443/443 verdes**.
- **Cleanup: remoção de `# DEBUG TEMPORARIO` em `account.py` (2026-05-23, parte 4 do loop)** — Bloco de 11 linhas no topo de `render_account_tab` exibia o JWT cru via `st.code(...)` para **qualquer plano** (incluindo Basic/free). Era redundante com a seção proper "Extensão Chrome — JWT" em `account.py:280+` que está gated por `billing.has_feature(plan, "live_monitor")` (planos Pro/Trial/admin). Em produção exibia segredo desnecessariamente para Basic (que não tem extensão) e duplicava o cartão para Pro/Trial/admin. Removido sem refactor adicional — a seção proper continua intocada. Grep `DEBUG TEMPORARIO|FIXME|XXX|HACK|TEMPORARIO` em `src/` retornou **0 matches** após o cleanup (estava sendo o último resquício). Suite continua **443/443 verdes** (zero teste tocava o bloco DEBUG).
- **Testes p/ `settings.py` (2026-05-23, parte 3 do loop)** — [tests/test_settings.py](tests/test_settings.py) com 12 testes em 2 classes cobrindo o único helper testável fora do contexto Streamlit. `PersistUserTzTests` (6 — payload exato `{"data":{"preferred_tz":tz}}` no `client.auth.update_user`, propaga em `st.session_state["user_tz"]`, sincroniza `session["user"]["user_metadata"]["preferred_tz"]` preservando outras chaves, cria `user_metadata` vazio quando ausente, skip defensivo quando `user` não é dict, retorna `(False, msg)` em exception **sem** popular session_state — ordem importa pra UI não mostrar falsa confirmação). `CommonTimezonesTests` (6 — sanity de lista/strings/non-empty, contém `PRIMARY_TZ` (ET), `FALLBACK_USER_TZ` (BRT) e `UTC`, zero duplicatas para dedup `dict.fromkeys`, ordem com `America/Sao_Paulo` no topo pro público-alvo brasileiro). Helpers Streamlit-bound (`_detect_browser_tz_component`, `_render_section_*`, `render_settings_tab`) ficam fora — JS injection + selectbox + number_input + button + rerun exigem contexto runtime. **Suite total: 431 → 443 verdes.**
- **Testes p/ `account.py` + sincronização kanban (2026-05-23, parte 2 do loop)** — Levantamento autônomo via 3 subagentes paralelos identificou: (a) kanban em `_gestao/BI_TopStep/` estava 2 entregas atrasado (`app_releases` 2026-05-21 e fix UI cinza 2026-05-23 ausentes de "Feito"), (b) relatório do Explore reportou 5 falsos positivos de "módulo sem teste" (todos já tinham), (c) único gap real era `account.py`. Criado [tests/test_account.py](tests/test_account.py) com 24 testes em 5 classes: `_fmt_money_cents` (5 — formato BR `$1.900,00 USD` com ponto-milhar/vírgula-decimal, custom currency, zero, milhão); `_fmt_date` (7 — None, ISO `Z`/offset, string inválida volta como veio, naive→UTC, aware tz, fallback `str()` para tipo inesperado); `_status_label` (3 — i18n known/unknown/None); `_load_subscription_row` (4 — row exists/no data/exception silenciosa/select inclui `stripe_customer_id` — sem ele a abertura do Customer Portal quebra); `_PLAN_CATALOG` (5 — slugs basic+pro, preços match seed `saas_schema.sql`, pro=unlimited, i18n keys, positive price). Helpers Streamlit-bound (`_render_plan_card`, `_trigger_checkout`, `_trigger_portal`, `render_account_tab`, `handle_checkout_return`) ficaram fora — exigem contexto Streamlit. Kanban e log em `_gestao/BI_TopStep/` sincronizados; dashboard pessoal continua precisando regen. **Suite total: 407 → 431 verdes.**
- **Bug latente em `live._fetch_*` (cache key vazada entre traders) + testes (2026-05-23)** — `@st.cache_data` em `_fetch_last_snapshot` e `_fetch_recent_alerts` em [src/live.py](src/live.py) usava `_user_id` (com underscore). No Streamlit, prefixo `_` em parâmetro de `cache_data` **desabilita o hash** — isso é convenção para tipos não-hasháveis (clientes DB), mas user_id é string. Resultado: dois traders no mesmo processo Streamlit compartilhariam a entrada de cache por 2s (TTL), expondo snapshot do outro user até o cache invalidar. Fix removeu o underscore, atualizou docstring explicando o porquê. Adicionado [tests/test_live.py](tests/test_live.py) com 12 testes cobrindo `_fetch_last_snapshot` (7: empty, 1 row, multi rows pega primeiro, exception não propaga, order desc, limit=1, table name, cache isolado por user_id) e `_fetch_recent_alerts` (5: delegação, default/custom limit, cache isolado). **Suite: 395 → 407 verdes.**
- **Merge `fusao/m5-daily-plans` → `main` + push (2026-05-23)** — merge commit `c82c18e` consolida 33 commits da fusão (M5→M11 + Live Monitor + smoke test). GitHub Secret Scanning bloqueou o push inicial por falso-positivo em `supabase/functions/stripe-webhook/.env.example:15` (`SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJ...service-role...` — `eyJ` prefix dispara detector de JWT mesmo sendo placeholder). Resolvido via "Allow secret" no painel de Security do GitHub. Branch local `fusao/m5-daily-plans` mantido para referência.
- **Fix Web Notifications Realtime auth (2026-05-23)** — `_inject_web_notifications` em [src/live.py:212](src/live.py#L212) usava `sb.auth.setSession({access_token, refresh_token: ""})` para autenticar Realtime; em supabase-js v2 essa call falha silenciosamente e o canal cai no `apiKey` (anon). RLS bloqueia eventos, `postgres_changes` nunca chega. Troquei por `sb.realtime.setAuth(jwt)` (API designada). Confirmado via DevTools (`phx_join` agora envia JWT com `role:authenticated, aud:authenticated, sub:<user>`) e teste end-to-end (INSERT em `public.alerts` → balão de notificação aparece no Windows em ~3s). Detalhe da pegadinha registrado na seção Gotchas.
- **Smoke test Live Monitor end-to-end OK (2026-05-23)** — 4 checkpoints do `PRD/SMOKE_TEST_live.md` passaram: (1) ping autenticado retorna `{"ok":true,...,"mode":"ping"}`; (2) snapshot real grava em `live_snapshots` com schema esperado (MES/Long/size=1/avg=5800.50); (3) Risk Guard trigger dispara alerta `daily_loss_limit/critical` quando `day_pnl <= -daily_loss_limit_usd` (50 USD configurado); (4) cooldown de 5min suprime segundo alerta do mesmo `(user_id, alert_type)`. Aba Live no Streamlit renderiza tudo (status/posição/PnL/alerts), Web Notifications: granted. **Pendente:** acoplar a extensão Chrome real (fim do Checkpoint 3 do smoke test que exige `topstepx.com/trade`) + limpeza dos alertas/risk_settings de teste.
- **Fix `live-ingest` 401 invalid token (2026-05-23)** — após migração Supabase para JWT signing keys assimétricos (ES256), `createClient(SUPABASE_URL, jwt)` falha porque o 2o arg é a **apikey**, não o JWT do usuário. Antes (HS256) tolerava por acaso; agora o gateway valida apikey separado e rejeita. Fix em [supabase/functions/live-ingest/index.ts:103](supabase/functions/live-ingest/index.ts#L103): usar `SUPABASE_ANON_KEY` (auto-injetada) como apikey + JWT do usuário via `global.headers.Authorization`.
- **Fix UI "tudo cinza/desabilitado" (2026-05-23)** — Streamlit aplica opacidade reduzida em elementos com `data-stale="true"` durante reruns. Em páginas com `st_autorefresh(3s)` (aba Live) o estado fica quase perpétuo, mascarando o contraste real. Override em [src/app.py:78-85](src/app.py#L78-L85) força `opacity:1` em qualquer elemento stale. Também subido `textColor` em [.streamlit/config.toml](.streamlit/config.toml) de `#e6e6e6` para `#f2f3f5` (polimento de contraste).

- **Loop autônomo (2026-05-22, parte 1)**: testes p/ `metrics.compute_groups` ([tests/test_metrics_groups.py](tests/test_metrics_groups.py)) — 15 testes em 6 classes cobrindo o motor de overlap grouping (a base de toda agregação por "operação"). Empty (2 — DF vazio retorna par vazio + coluna group_id int64 presente), Single trade (2 — group_id=1, additions=0, has_addition=False; agregações conservam contract/type/total_points/total_pnl/total_net_pnl/total_size + group_start/end), Overlap (4 — 2 trades disjuntos = 2 grupos, 2 trades com overlap = 1 grupo, extensão dinâmica do cur_end com 3 trades em cascata + 4º disjunto, boundary exato `entered==exited` mantém no mesmo grupo — documenta semântica do `>` vs `>=`), Isolation (2 — contracts diferentes nunca mergem mesmo com overlap temporal; types Long/Short opostos hedge separado), Ordering (1 — input desordenado é sortado por entered_at), Status (2 — Winner/Loser/Flat baseado em totals agregados, não trade-a-trade), Duration (2 — `duration_min` usa span outer do grupo, não soma de trades). Helper `_make_trades(rows)` aplica defaults sensatos (points=0/pnl=0/pnl_net=0/size=1) só preenchendo o que falta. **Suite total: 262 verdes** (eram 247).
- **Loop autônomo (2026-05-22, parte 8)**: testes p/ `auth.py` ([tests/test_auth.py](tests/test_auth.py)) — 25 testes em 10 classes cobrindo autenticação Supabase. `current_user` (2 — none/dict); `current_user_id` (3 — none, string, KeyError documentado quando session mal-formado); `_read_secret` (5 — st.secrets first, multi-name cascata, fallback env var, None quando vazio, swallow FileNotFoundError de secrets); `_apply_session_to_client` (2 — set_session normal, fallback postgrest.auth on exception); `_session_to_dict` (3 — extract basic, id coerced to str, metadata None→{}); `_redirect_url` (2 — APP_URL custom, localhost fallback); `_sign_in_email` (2 — sucesso persiste em state, falha devolve err); `_sign_up_email` (3 — com session imediata, sem session devolve confirm_email, falha devolve err); `get_client` (2 — anon sem session, aplica set_session com sess); `_t` wrapper (1 — delega para i18n.t sem ciclo). Padrão `FakeSessionState(dict)` mimica `st.session_state`. **Suite total: 395 verdes** (eram 370). Skipped: `login_screen`, `_sign_in_google`, `_handle_oauth_callback`, `sign_out` (UI heavy / chamam `st.rerun` que para o test runner).
- **Loop autônomo (2026-05-22, parte 7)**: testes p/ `metrics.compute_coach` ([tests/test_metrics_coach.py](tests/test_metrics_coach.py)) — 25 testes em 9 classes cobrindo o orquestrador comportamental e seus 8 sub-helpers privados. Top-level (2 — empty schema completo de 10 chaves, smoke test com DF mínimo); Revenge (4 — <2 trades, sem losses retorna baseline, janela 5min detecta trade pós-loss-grande, exclui trades distantes); CutHold (4 — ratio=0 sem wins, ratio=0 sem losses, flag=True quando ratio>=2.0, no-flag quando <2.0); Overtrading (2 — empty zerado, threshold=p75 de trades/dia identifica tilt days); LosingStreak (5 — empty, no_losses, single loss=length 1, 3 consecutivos pegos, 2 streaks devolve o mais longo); Combo (3 — `LEAK_MIN_TRADES=3` exclui combinações pequenas, leak filtra pnl<0, strength filtra pnl>0); SizeBuckets (2 — sem coluna size devolve vazio, groupby + agg correto); PointsDist (3 — vazio, mean/median, dropna). Helper `_prep_for_private` adiciona `duration_sec` (feito por `compute_coach` antes de chamar privados). **Suite total: 370 verdes** (eram 345). **Marco**: `metrics.py` agora tem cobertura completa das 8 funções públicas (groups/kpis/segments/daily/overview/duration_buckets/coach + plan_adherence já existia).
- **Loop autônomo (2026-05-22, parte 6)**: testes p/ `metrics.compute_overview` ([tests/test_metrics_overview.py](tests/test_metrics_overview.py)) — 22 testes em 7 classes cobrindo o dict de KPIs em USD do painel "Visão geral". Empty (1 — schema 22 chaves zerado, best/worst day+trade = None); Counts (4 — trade_count, win/loss particionado por pnl_net sign, total_lots soma `size`, robusto a NaN via `to_numeric+fillna(0)`); Pnl (5 — total_pnl_net assina, avg_winning só sobre winners, **avg_losing preserva sinal negativo** (diferente de compute_kpis que usa abs), edge cases sem winners ou sem losers); Duration (3 — avg_trade mean global, partição win/loss, zero quando subset vazio); Daily (5 — winning_days conta dias com soma > 0, agregação multi-trade no mesmo dia, best/worst_day como tuple `(date, pnl)`, best_day_pct=best/total, zero protegido quando total <= 0); Direction (2 — long/short count + pct, only-longs → short_pct=0); BestWorstTrade (2 — dict com 9 chaves preservando contract/type/sizes/prices/entered_at/exited_at, ambos compartilham schema). Helper `_trades(rows)` aceita `duracao_sec` ou `entered_at/exited_at` explícitos. **Suite total: 345 verdes** (eram 323).
- **Loop autônomo (2026-05-22, parte 5)**: testes p/ `metrics.compute_duration_buckets` ([tests/test_metrics_duration_buckets.py](tests/test_metrics_duration_buckets.py)) — 13 testes em 5 classes cobrindo os 11 buckets de duração do TopStepX (`Under 15 sec` até `4 hours and up`). Empty (1 — DF vazio devolve schema); Shape (2 — sempre 11 linhas em ordem fixa quando df não-vazio, buckets sem trades zerados); Classification (5 — under_15, lower boundary inclusiva `[lo, ...)`, upper boundary exclusiva `..., hi)` documenta semântica, "4 hours and up" open-ended pega 5h/10h/24h, midpoints de cada bucket geram 1 trade em cada um dos 11); WinRate (4 — flat com `pnl_net=0` entra em trades mas não em wins, full wins=1.0, no wins=0.0, sem divisão-por-zero nos 10 buckets vazios); Types (1 — trades/wins int, win_rate float). Helper `_trades` constrói entered_at fixo + exited_at via Timedelta. **Suite total: 323 verdes** (eram 310).
- **Loop autônomo (2026-05-22, parte 4)**: testes p/ `metrics.compute_daily` ([tests/test_metrics_daily.py](tests/test_metrics_daily.py)) — 13 testes em 5 classes cobrindo daily breakdown. Empty (1 — DF vazio retorna estrutura com 8 colunas esperadas); SingleDay (3 — 1 trade, win+loss partitioned em mesma data, flat com points=0 entra em total_size mas fora de winning/losing_size); MultipleDays (3 — 2 dias = 2 linhas, output sorted ascending por trade_day, groupby isola corretamente); RewardRisk (3 — rr=w_pts/abs(l_pts), edge cases sem losers e sem winners protegidos); DayCol (3 — prefere `trade_day_et` quando ambos presentes mesmo com datas diferentes, fallback para `trade_day` legado, output sempre renomeia para `trade_day` mesmo vindo de `trade_day_et`). Helper `_trades(rows, et_col=True)` permite alternar entre path moderno e legado. **Suite total: 310 verdes** (eram 297).
- **Loop autônomo (2026-05-22, parte 3)**: testes p/ `metrics.compute_segments` ([tests/test_metrics_segments.py](tests/test_metrics_segments.py)) — 13 testes em 5 classes cobrindo os 4 buckets (`no_additions`, `with_additions`, `with_additions_winners`, `with_additions_losers`). Empty (1 — 4 buckets zerados com schema completo de 8 chaves); Partitioning (4 — só sem adição popula no_additions; só com adição popula 3 buckets; Flat com adição entra em with_additions mas fora dos sub-buckets de win/loss; mix particionado corretamente); Aggregation (3 — total_points/total_pnl/total_size somam dentro do bucket; avg_points/avg_pnl usam mean do bucket; avg_additions só do subset); WinRate (3 — relative ao bucket, sub-buckets sempre 1.0 ou 0.0 por definição, only-flats=0.0); Schema (2 — 4 buckets com chaves idênticas, int só para count, float para o resto). **Suite total: 297 verdes** (eram 284).
- **Loop autônomo (2026-05-22, parte 2)**: testes p/ `metrics.compute_kpis` ([tests/test_metrics_kpis.py](tests/test_metrics_kpis.py)) — 22 testes em 6 classes cobrindo as 13 chaves do dict de KPIs em pontos. Empty (2 — schema completo zerado, ignora groups quando df vazio); Counts (3 — trade_count=len, particiona winners/losers por sign, flats com points=0 ficam fora de winners E losers); Sums (3 — total_net assina, separa win/loss, avg_points_per_trade usa todo df incluindo flats); Averages (4 — avg_winning só sobre winners, avg_losing é `abs(mean(losers))`, edge cases sem winners ou sem losers); RrRatio (4 — rr_average=mean_win/abs(mean_loss), rr_aggregate=sum_win/abs(sum_loss), divisão-por-zero protegida em ambas as direções); Grouped (4 — win_rate_grouped conta só "Winner", groups vazio com df cheio retorna 0.0, full-win=1.0, no-win=0.0 mesmo com Flat); Schema (2 — chaves estáveis entre empty e non-empty, tipos: int para counts e float para o resto). **Suite total: 284 verdes** (eram 262).
- **Loop autônomo pós-deploy (2026-05-21, parte 4)**: 5 frentes entregues sem depender dos passos manuais bloqueados.
  (i) **Stacked bar de aderência** ([src/app.py:738](src/app.py#L738)) — horizontal stacked bar plotly mostrando proporção das 5 categorias dentro do expander "Aderência ao plano matinal"; reusa `PLOTLY_LAYOUT`, paleta dedicada (GREEN/RED/#f4a261/#9b2226/#e9c46a), labels só quando ≥8% para não poluir; sem legend (categorias já listadas no card ao lado).
  (ii) **Botão "Limpar dia"** em Day Plan com confirmação 2-cliques via `st.session_state["_day_plan_pending_clear"]`. Novo `daily_plan.delete_plans_for_date(plan_date)` retorna `{ok,deleted,error}`. 8 chaves i18n novas × 3 idiomas.
  (iii) **Refactor extracted Supabase client**: novo `auth.current_user_id()` público; 5 módulos (daily_plan, action_plan, risk_settings, live, app via coach_ai) migrados de `_supabase()/_current_user_id()` para `auth.get_client()/auth.current_user_id()`. Wrappers em `coach_ai.py` mantidos como aliases delegando para auth (backward compat). `live.py` perdeu sua função `_supabase` local. Reader benefit: módulos CRUD agora importam de `auth` direto (sem o "por que está em coach_ai?").
  (iv) **Audit do `guia_execucao.md`** — adicionada seção §0.5 "Status de execução" com 4 sub-seções: entregue conforme plano (tabela por fase), extras entregues além do plano (i18n da ext em 3 idiomas, M11, hardening da live-ingest, validateJwt, runbook smoke, copy/clear day, stacked bar, refactor auth), pendente manual (4 itens), backlog (M-2 vendor, OAuth, CWS, partitioning, etc.).
  (v) **Testes p/ daily_plan CRUD** ([tests/test_daily_plan_crud.py](tests/test_daily_plan_crud.py)) — 9 testes mockando `auth.get_client()` via `unittest.mock` para cobrir `last_planned_date_before` (2), `delete_plans_for_date` (3 — sucesso/zero/exceção) e `copy_plans` (4 — clona, pula colisões, source vazio, sem auth).
  (vi) **Testes p/ risk_settings** ([tests/test_risk_settings.py](tests/test_risk_settings.py)) — 15 testes cobrindo `get_settings` (3 — linha existe / não existe / exception silenciosa), `upsert_settings` (5 — bloqueia sem auth, injeta user_id, on_conflict=user_id, exception, payload não mutado), `apply_account_defaults` (6 — 4 perfis + fallback Custom para desconhecido + isolation do dict retornado evitando shared reference bug), `ACCOUNT_TYPES` constant (2 — match com defaults.keys + ordem preservada para SelectboxColumn da UI).
  **Suite total: 57 verdes** (eram 33 antes do batch, 42 após o (v)).
- **Loop autônomo pós-deploy (2026-05-21, parte 5)**: 2 entregas.
  (i) **Testes p/ `alerts.py`** ([tests/test_alerts.py](tests/test_alerts.py)) — 13 testes cobrindo `list_recent` (6 — empty / dataframe coercion de datetime UTC / default limit 50 / custom limit / order desc / exception silenciosa), `mark_read` (3 — patch read_at ISO UTC / coerção str→int de alert_id / exception), `mark_dismissed` (1), `mark_all_read` (2 — filter `is_(read_at, null)` para não sobrescrever timestamps válidos / exception), `_now_iso` (1 — formato ISO UTC dentro de 1min do agora). **Suite total: 70 verdes**.
  (ii) **Audit do `DECISOES.md`** — adicionada entrada 2026-05-21 "Hardening pré-deploy + reversões de escopo do loop autônomo" com 8 decisões formalmente registradas (i18n 3 idiomas reverteu decisão #22 de 20/05; M11 UNIQUE+upsert; rate limit em memória; logs estruturados sem lib externa; validateJwt no popup sem verificar HMAC client-side; refactor `_supabase`→`auth.*`; Day Plan copy/clear; stacked bar inline). 4 alternativas consideradas e descartadas + consequências mapeadas.
- **Loop autônomo pós-deploy (2026-05-21, parte 6)**: 3 entregas. (i) **Testes p/ `action_plan.py`** ([tests/test_action_plan.py](tests/test_action_plan.py)) — 16 testes cobrindo `list_items` (3 — schema previsível, sort por (done, prio, due), coerção de due_date), `_normalize_row` (7 — basic + sync done↔status bidirecional + invalid enum fallback + NaT/empty string due_date + missing task), `upsert_items` (6 — insert/update/delete, ignora linhas em branco, no-diff sem update, sem auth bloqueia). (ii) **Testes p/ `metrics._day_col`** ([tests/test_metrics_helpers.py](tests/test_metrics_helpers.py)) — 5 testes garantindo preferência por `trade_day_et` sobre `trade_day` (crítico para Dashboard não misturar fusos pós-fusão). (iii) **Fix `pd.Timestamp.utcnow()` deprecated** em `action_plan.py` + `daily_plan.py` (pandas 4 já avisa em pytest output). Migrado para `pd.Timestamp.now("UTC")`. **Suite total: 91 verdes** (eram 70).
- **Loop autônomo pós-deploy (2026-05-21, parte 7)**: tests p/ `ingest_core.py` ([tests/test_ingest_core.py](tests/test_ingest_core.py)) — 31 testes cobrindo `detect_format` (4), `_clean_money` (6), `_duration_to_pg_interval` (5 — incluindo truncate de fração .NET 7 dígitos), `normalize_topstepx` (6 — rename, dtypes, dedup keep_last, UTC, microsec truncate), `records_for_supabase` (4 — injeta user_id, ISO timestamps, não muta input), `upsert_batches` (3 — single/múltiplos batches/vazio), `_synthetic_id` (3). **Suite total: 122 verdes** (eram 91).
- **Loop autônomo pós-deploy (2026-05-21, parte 8)**: tests p/ `billing.py` ([tests/test_billing.py](tests/test_billing.py)) — 33 testes cobrindo `has_feature` (7 — truthy/falsy/missing/None plan/None features/empty dict/string truthy), `feature_value` (4 — set/default/None plan/no default), `_parse_ts` (7 — None/datetime tz-aware/naive→UTC/ISO/Z suffix/invalid string/invalid type), `days_left_in_trial` (5 — None plan, não-trialing, sem ends_at, futuro 5d, passado=0), `is_trial_expired` (5 — free+free/free+pro/trialing/active/None), `is_admin` (3 — user in/out/exception via patch direto em `auth.get_client`), `ensure_subscription` (2 — chama RPC, swallow exception). **Suite total: 155 verdes** (eram 122). Gotcha aprendido: `patch("billing.auth")` não funciona em `import auth` tardio — usar `patch("auth.get_client")` que afeta o módulo real em `sys.modules`.
- **Loop autônomo pós-deploy (2026-05-21, parte 9)**: tests p/ `reconstruct_dashboard_trades` (FIFO multi-leg matcher do CSV TopStep Dashboard legacy) — [tests/test_reconstruct_dashboard.py](tests/test_reconstruct_dashboard.py) com 20 testes em 7 classes. Cobertura: roundtrip simples Buy→Sell/Sell→Buy (2); multi-leg open+close (2 — N opens 1 close split proporcional + 1 open N closes mesmo entry); partial close + flip (3 — sobra fica aberta, flip cria novo open lado oposto, close exato); fees (3 — combina open+close per unit, abs() em valores negativos, commissions sempre 0); isolation por Product (2 — MNQ/MES não cruzam, dois products independentes); time handling (2 — strip de " CT"/" ET", sort de input desordenado); edge cases (5 — só opens retorna vazio, Side case-insensitive, trade_day vem do close, synthetic ids únicos, duration computada); integração via `normalize_dashboard` (1). **Suite total: 175 verdes** (eram 155). Garante que o caminho legado (Dashboard CSV) continua funcionando — alto valor porque a lógica é complexa e quebrar silenciosamente significa trades sumindo ou duplicados na ingestão.
- **Loop autônomo pós-deploy (2026-05-21, parte 10)**: dois módulos cobertos.
  (i) **`i18n.py`** ([tests/test_i18n.py](tests/test_i18n.py)) — 23 testes: `t()` lookup + fallback de lang + fallback para chave + interpolação com/sem kwargs (5); `current_lang()` com/sem session (2); `status_label`/`priority_label` known/unknown (4); roundtrip `status_label`↔`status_from_label` para os 3 status canônicos + idem para 3 priorities (3); `status_options`/`priority_options` (2); `weekday_label` com case-insensitive (2); integridade de constantes (4 — STATUS_DB_TO_KEY/PRIORITY_DB_TO_KEY/LANGS/DEFAULT_LANG). Streamlit session_state mockada com FakeSessionState.
  (ii) **`coach_ai.py`** ([tests/test_coach_ai.py](tests/test_coach_ai.py)) — 24 testes: `_lookup` (3 — chave missing, conhecida, fallback lang); `_extract_checklist` (8 — captura seção 4, seção 2 quando sem 4, prioridade 4 > 2, suporte * • -, listas numeradas, fallback any-bullet, sem bullets, cap em 10); `_format_history_block` (3 — vazio, com checklist extraído, fallback texto cru truncado em 300); `save_analysis` (4 — bloqueia texto vazio, sem user, insert full payload com user_id+período+contratos+result_filter, exception); `fetch_history` (4 — contratos vazios skip roundtrip, filtro `overlaps`, limit default 20, exception silenciosa); aliases legados `_supabase`/`_current_user_id` (2 — delegam para auth). **Suite total: 222 verdes** (eram 175). Cobertura agora cobre todos os módulos puros do core: metrics, daily_plan, action_plan, risk_settings, alerts, ingest_core, billing, i18n, coach_ai, timezones, reconstruct_dashboard.
- **Feature: app_releases (2026-05-21, parte 11)** — catálogo de versões da extensão Chrome e do app. 5 entregas:
  (i) Migration [PRD/m12_app_releases.sql](PRD/m12_app_releases.sql) cria `public.app_releases` (id, component CHECK in ('extension','app'), version, released_at, release_notes, download_url, is_latest, created_by). Partial UNIQUE index `is_latest=true` por componente garante 1 latest único; index `(component, released_at desc)` para listar histórico. RLS: SELECT a qualquer auth; INSERT/UPDATE/DELETE só a quem está em `public.admin_users`. Seed da extensão v0.1.0 com is_latest=true.
  (ii) Módulo [src/app_releases.py](src/app_releases.py) — `_parse_semver`, `compare_versions(installed, latest)` → `"up_to_date"|"outdated"|"ahead"|"unknown"`, `get_latest(component)`, `list_releases(component, limit)`. Falhas silenciosas (tabela inexistente devolve None/[]) para não quebrar a aba Live pré-deploy.
  (iii) UI: `_section_install()` em [src/live.py](src/live.py) ganhou badge `st.info` mostrando "Latest available: vX.Y.Z (released YYYY-MM-DD)" + expander com release_notes + botão `Download vX.Y.Z` se `download_url` estiver preenchido. Aparece logo antes do passo-a-passo de instalação.
  (iv) 3 chaves i18n × 3 idiomas: `live.install.latest_version`, `live.install.release_notes_label`, `live.install.download_btn`.
  (v) [tests/test_app_releases.py](tests/test_app_releases.py) — 25 testes: `_parse_semver` (6 — basic, v-prefix, whitespace, None/empty/inválido); `compare_versions` (8 — equal/lower patch/minor/major, ahead, inválido, None, v-prefix); `get_latest` (5 — row existe, sem row, componente inválido skip roundtrip, exception silencioso, default extension); `list_releases` (5 — order desc/limit default 10/custom limit/componente inválido/exception); `COMPONENTS` constant (1). **Suite total: 247 verdes** (eram 222).
  **Pendente manual:** rodar `PRD/m12_app_releases.sql` no Supabase + (depois do deploy) atualizar `download_url` apontando para `https://qjzouhdrhoxmtinidsqv.supabase.co/storage/v1/object/public/extension/extension-latest.zip`.
- **Polimentos pré-deploy (2026-05-20, parte 3)**: aba **Plano do Dia** ganhou botão "Copiar do dia anterior" em [src/app.py:1122](src/app.py#L1122) que chama `daily_plan.last_planned_date_before(target)` + `daily_plan.copy_plans(from_date, to_date)` (em [src/daily_plan.py](src/daily_plan.py)). Detecta automaticamente a data mais recente com planos < data selecionada, clona contract/direction/max_size/stops/targets/notes/entry_trigger; linhas já existentes para `(contract, direction)` no destino são puladas (UNIQUE constraint do banco protege como backstop). 6 chaves i18n novas nos 3 idiomas — testes de consistência continuam verdes.
- **Polimentos pré-deploy (2026-05-20, parte 2)**: (i) logs estruturados JSON em `supabase/functions/live-ingest/index.ts` com 11 eventos (`ping_ok`, `ingest_ok`, `auth_*`, `rate_limited`, `payload_too_large`, `invalid_*`, `missing_field`, `db_error`); `raw` do snapshot nunca é logado (privacidade). README detalha schema e filtragem via Logflare; (ii) `tests/test_i18n_consistency.py` (5 testes verdes) trava regressão de chaves divergentes entre `locales/{en,pt_BR,es}.json` e `extension/_locales/{en,pt_BR,es}/messages.json`, inclui check de placeholders alinhados (`$EMAIL$` etc.) e nenhum valor vazio; (iii) `PRD/SMOKE_TEST_live.md` em 4 checkpoints (Edge Function viva → extensão conecta → snapshot fluindo → Risk Guard dispara com cooldown) para validar end-to-end depois dos 3 deploys manuais.
- **M11 aplicada + hardening pré-deploy (2026-05-20)**: `PRD/m11_live_snapshots_unique.sql` rodada no Supabase com sucesso. UNIQUE `(user_id, account_id, snapshot_at)` ativa — Edge Function `live-ingest` agora é idempotente de verdade (upsert + ignoreDuplicates funcionam contra retry da extensão). **Hardening adicional**: (i) `live-ingest` ganhou rate limit em memória 12 req/min por user_id (429), cap de 16KB no body (413), validação de skew de ±5min em `snapshot_at` (400) e clamp de ranges para position_size, PnL/drawdown, comprimento de account_id/contract; (ii) `extension/popup.js` ganhou `validateJwt(jwt)` que checa shape (3 partes base64url + header HS*), claims obrigatórias (`sub` uuid, `exp`/`iat` numéricos, `aud=authenticated`) e `exp` > now antes de exibir email/expira — botão Save bloqueia JWT inválido/expirado com mensagem específica via 2 chaves i18n novas (`popupTokenInvalid`, `popupTokenExpired`) nos 3 idiomas. **Pendente manual** (bloqueado pelo classificador): criar bucket público `extension` no Supabase Storage, subir `dist/extension/extension-latest.zip`, `supabase login` + `supabase functions deploy live-ingest --no-verify-jwt`.
- **`guia_execucao.md` da fusão pronto (2026-05-20)**: roteiro detalhado em ~30 tarefas auto-contidas (T0 a T4.4) cobrindo Fase 1.3 (schemas Postgres novos), Fase 1.2 (dual-timezone NY/ET + aba Configurações), Fase 1.1 (refinamento de adições não-planejadas), Fase 2 (extensão Chrome MV3 + Edge Function `live-ingest`), Fase 3 (real-time via polling + Risk Guard via trigger Postgres + Web Notifications) e Fase 4 (cleanup do Trade_Agent).
- **Fase 1.3 codada e aplicada (2026-05-20)** — 7 migrations `PRD/m6_*.sql` + `m6_README.md` executadas com sucesso no Supabase; `daily_plan.py` lê `contracts` do banco (fallback offline ativo). M6 migrations refeitas em padrão `DROP + CREATE` (m5 style) após primeira tentativa esbarrar em tabela legada do Trade_Agent local.
- **Fase 1.2 codada (2026-05-20)** — dual-timezone NY/ET + aba "Configurações" prontos (`timezones.py`, `settings.py`, integração em `app.py` + `metrics.py`, i18n nos 3 idiomas).
- **Fase 1.1 codada (2026-05-20)** — `compute_plan_adherence` com 5 categorias (`compliant`, `unplanned`, `size_exceeded`, `against_plan`, `size_creep_day`); UI + i18n.
- **Fase 2 codada (2026-05-20)** — Extensão Chrome MV3 + Edge Function `live-ingest` + aba Live (Pro) + botão JWT em Account + `scripts/package_extension.py`. **`m9_features.sql` aplicado.**
- **Fase 3 codada (2026-05-20)** — Risk Guard trigger, CRUD `risk_settings.py` + UI, CRUD `alerts.py` + UI rica na aba Live, Web Notifications.
- **Fase 4 codada (2026-05-20)** — Post-mortem completo em `PRD/ENCERRAMENTO_trade_agent.md` (cópia de referência). **Pendente:** copiar para raiz do Trade_Agent, adicionar warning no README dele, criar tag `v-archived` (passos detalhados na seção 12 do ENCERRAMENTO). Toda a fusão Trade_Agent → BI TopStep está codada. Próximos passos operacionais: aplicar `PRD/m10_risk_guard_trigger.sql`, deploy `supabase functions deploy live-ingest`, subir extensão `.zip` no Supabase Storage, validar end-to-end, arquivar Trade_Agent.
- **Fusão com Trade_Agent aprovada (2026-05-20)**: este repo passa a absorver o projeto `E:\BD\Trade_Agent` (sistema local real-time anti-tilt, em pré-Fase-1). Migração em 4 fases: (0) pré-reqs — ✅ M4 Stripe webhook mergeado em `saas-main` (merge commit `6df8737`); falta snapshot final do Trade_Agent; (1) batch — portar detecção de adições não-planejadas + janelas dual-timezone (Brasília/Chicago) + schema novo (`live_snapshots`, `alerts`, `tilt_patterns`, `daily_plans`, `payouts`); (2) extensão Chrome MV3 multi-tenant (auth via Supabase JWT); (3) real-time via Supabase Realtime + Web Notifications API + Risk Guard em Edge Function; (4) cleanup (arquivar Trade_Agent, gerar `ENCERRAMENTO.md` lá). **Real-time é MVP** da fusão. Coach IA segue copy-paste (sem LLM no SaaS). Plano completo: `C:\Users\henrique.tamaki\.claude\plans\me-fa-a-um-comparativo-soft-bear.md`. Decisão detalhada em `DECISOES.md`.
- **App SaaS**: **M4 concluído (2026-05-20)** — Edge Function `stripe-webhook` mergeada em `saas-main` (merge commit `6df8737`, parents `9fd0c26 4978379`). Branch `saas/m4-stripe-webhook-edge-fn` preservada como referência histórica; pode ser deletada quando staging confirmar comportamento. Próximo passo: deploy em ambiente Supabase + cadastro do endpoint no Stripe Dashboard com os 6 eventos documentados em `supabase/functions/stripe-webhook/README.md`. Schema multi-tenant e RLS já aplicados.
- **Refatoração de layout (2026-05-20)**: todos os 10 módulos `.py` da raiz foram movidos para `src/` via `git mv` (preserva histórico). Imports planos (`import auth`, `from i18n import t`) continuam funcionando porque Streamlit adiciona o diretório do script ao `sys.path`. Ajustes em `Path(__file__).resolve().parent` → `.parent.parent` em `auth.py`, `i18n.py`, `app.py` e `ingest.py` (esses resolviam `Env/` e `locales/` relativos ao próprio arquivo). `.bat` e docs apontam agora para `src/app.py` / `src/ingest.py`. `login-screen.png` foi para `assets/`. Raiz contém apenas: 3 `.bat`, `requirements.txt`, `.gitignore`, docs (`README.md`, `CLAUDE.md`, `MEMORIA.md`, `DECISOES.md`) e pastas de infraestrutura. **Atenção em deploy**: Streamlit Cloud precisa ser reconfigurado para entrypoint `src/app.py`.
- **Indicador customizado TopstepX (anexo)**: `PRD/topstepx_pullback_mnq.user.js` **v0.3.0** (refactor amplo). Mantém todas as features de 0.2.x (EMAs 9/21/50/200, VWAP CME, Bone Zone, AlgoAlpha local, volume SMA, plotshapes A+/B/C, linhas Entry/Stop/Alvos, log + beep). Mudanças: hot path mais rápido (cache de boundary VWAP elimina `Intl.DateTimeFormat` por barra; drop de `new_var` ociosos), bootstrap robusto (detecção por shape + soft-watch contínuo + reativação em SPA navigation), sanidade de runtime (clamp de inputs, feature-detect de `PineJS.Std.*`, reset em troca de símbolo/TF, TTL configurável das linhas, AudioContext singleton). Novo input `linhasTTLBars` (default 50, 0=infinito). Trilha de versões em `PRD/CHANGELOG_pullback_mnq.md`.

## Como rodar / testar / debugar

### App (Streamlit + Supabase)

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
streamlit run src/app.py
```

Schema em `PRD/schema.sql`, aplicado manualmente no SQL Editor do Supabase.

### Indicador customizado no TopstepX

1. Instalar Tampermonkey no Chrome.
2. Criar novo userscript no Tampermonkey, colar o conteúdo de `PRD/topstepx_pullback_mnq.user.js`, salvar (Ctrl+S).
3. Abrir página de trade do TopstepX → menu Indicators → buscar "Pullbacks MNQ".
4. Diagnóstico: F12 → Console → buscar prefixo `[Pullback MNQ]`. A última linha deve ser `custom indicator injetado`.

## Gotchas / Aprendizados

- 2026-05-23 — **`@st.cache_data` ignora o hash de parâmetros com prefix `_`**. Convenção do Streamlit pensada para tipos não-hasheáveis (clientes DB etc.). Mas se você usa `_user_id: str` por engano em uma função cacheada, **dois traders no mesmo processo Streamlit compartilham a entrada de cache por todo o TTL** — o segundo trader vê dados do primeiro até o cache invalidar. Sintoma: dados corretos no primeiro login da sessão; o segundo login do mesmo processo (raro em produção single-user, mas frequente em smoke local e em hosting multi-tenant) vê snapshot do primeiro user. RLS não protege porque o vazamento é em memória dentro do app, não no banco. **Fix:** renomear para `user_id` (sem underscore) e aceitar o "type warning" do Streamlit — string é hasheável. Caso real: `live._fetch_last_snapshot` + `_fetch_recent_alerts` (commit `ee99800`). **Regra prática:** em qualquer função cacheada que toque user_id, conferir que ele NÃO começa com underscore e que aparece na chave de cache (logar `hash(user_id)` em dev).
- 2026-05-23 — **Supabase pós-migração de signing keys (ES256/assimétricos): o 2º arg de `createClient(URL, ...)` é `apikey`, NÃO o JWT do usuário**. Antes (HS256/HMAC com `service_role` ou `anon` como apikey), passar o JWT do usuário ali funcionava por acaso — o gateway tolerava porque a apikey era validada com a mesma chave secreta do JWT. Com chaves assimétricas, o gateway valida apikey separado pela public key do projeto e rejeita JWT do usuário com `401 invalid token`. **Fix:** sempre `SUPABASE_ANON_KEY` (auto-injetada em Edge Functions Deno como env var) como apikey + JWT do usuário em `global.headers.Authorization`. Caso real: [supabase/functions/live-ingest/index.ts:103](supabase/functions/live-ingest/index.ts#L103) (2026-05-23). **Sintoma:** Edge Function passa autenticação local (`getUser(jwt)` retorna user) mas qualquer INSERT/SELECT subsequente retorna 401, mesmo com RLS permissiva. Olhar o header `apikey` no curl: se for o JWT do usuário, é esse bug.
- 2026-05-23 — **SQL Editor do Supabase roda como `postgres` (service_role) — `auth.uid()` retorna NULL ali**. Qualquer `select ... where user_id = auth.uid()` devolve zero linhas mesmo com dados presentes. **Não tem como "logar" no Editor como user comum** — é caminho legítimo para inspecionar dados que precisa filtrar pelo UUID literal (`where user_id = 'c4765210-...'`). Confirmar via `select auth.uid();` (retorna NULL). Custou ~30 min de debug em 2026-05-21 antes de cair a ficha. Também documentado no [CLAUDE.md](CLAUDE.md) seção "Notas operacionais" mas repetido aqui por valor de exposição.
- 2026-05-23 — **supabase-js v2 Realtime: usar `sb.realtime.setAuth(jwt)`, não `sb.auth.setSession({access_token, refresh_token: ""})`** para autenticar canais como user logado. Em [src/live.py](src/live.py) o JS de Web Notifications fazia `setSession` com refresh_token vazio; a chamada falha silenciosamente em v2 (refresh_token "" é inválido) e o canal cai no fallback `apiKey` (anon). RLS então aplica `auth.uid()=NULL` na tabela `alerts` e nenhum `postgres_changes` chega — subscription fica viva (vê `phx_join` ok + `system "Subscribed to PostgreSQL"`) mas sem broadcast. Sintoma: Web Notification nunca dispara mesmo com tabela publicada em `supabase_realtime`, RLS correto, JWT correto chegando no iframe. **Debug:** olhar `access_token` na `phx_join` do WS (DevTools → Network → WS → Messages); se decodifica para `role: anon` em vez de `role: authenticated`, é esse bug. Fix em [src/live.py:212](src/live.py#L212).
- 2026-05-21 — **`.venv` quebra ao mover a pasta do projeto** entre drives/paths (ex.: `C:\Users\...\OneDrive\BD\...` → `E:\BD\...`). Os shims `pip.exe`/`streamlit.exe` no `.venv\Scripts\` têm o path do `python.exe` **hardcoded no binário** (Windows launcher), não são symlinks. Sintoma: `Fatal error in launcher: Unable to create process using '<old path>'`. **Fix:** apagar `.venv` e recriar com `python -m venv .venv` na nova localização. Workaround temporário: chamar `\.venv\Scripts\python.exe -m pip ...` em vez de `pip.exe` (o python.exe em si é cópia self-contained, não tem o path hardcoded).
- 2026-05-21 — **pip 26.1.1 é quebrado no Windows + Python 3.14**: `OSError: [Errno 22] Invalid argument` em `pip\_vendor\distlib\resources.py:171` ao tentar qualquer `pip install`. Vem do upgrade via `python -m pip install --upgrade pip`. **Fix:** ficar com a versão bundled do `venv` (pip 26.0.1 na época). Não rodar upgrade preventivo de pip em venv novo até a 26.1.x estabilizar. Reproduzido em Python 3.14.4 / Windows 11.
- 2026-05-20 — `Intl.DateTimeFormat(...).formatToParts()` é caro o suficiente para dominar o tempo por barra em backfills longos (~5-20µs/chamada + alocação). Para indicadores que precisam de timezone (ex.: sessão CME 18:00 ET), **cachear o boundary** (próximo limite em ms epoch) e só recomputar quando o timestamp ultrapassa. DST é tratado naturalmente porque a primeira barra após transição recomputa e corrige.
- 2026-05-20 — `ctx.new_var()` aloca + mantém histórico do valor — só vale a pena para séries lidas com `.get(n)`. No port v0.2.x, `high_s` e `low_s` foram criados mas nunca consumidos: drop dessas duas chamadas é ganho gratuito por barra. Sempre auditar `.get(` no arquivo antes de mover OHLCV para new_var.
- 2026-05-20 — A charting_library v31 recria o contexto do custom indicator quando o usuário muda inputs no dialog (cache de inputs em `init` é redundante), mas **não** sempre quando troca o símbolo/timeframe — precisa detectar manualmente via `ctx.symbol.ticker + period` e resetar contadores/VWAP/sinal confirmado para evitar contaminação cross-symbol.
- 2026-05-20 — **Shape do widget `tradingview_*` da TopstepX é mínimo**: expõe **só** `getCustomIndicators`. Nenhuma das 9 sondas testadas (`chart`, `subscribe`, `setSymbol`, `_options`, `_innerAPI`, `activeChart`, `symbolInterval`, `onChartReady`, `addCustomIndicator`) está presente. Implicação: a detecção `looksLikeWidgetCfg` no userscript **deve** ficar minimalista (só `getCustomIndicators`) — tentar endurecer com shape checks adicionais quebra silenciosamente (foi o que aconteceu no primeiro corte da v0.3.0; corrigido antes do commit). Mantemos um `describeCfgShape` que loga as sondas presentes no momento do patch (`cfg shape: ...`) só para detectar **mudanças futuras** no widget sem precisar reincorporar checks no caminho crítico.
- 2026-05-20 — Userscripts com `@match` largo (`/*`) + `setupSPAObserver` (monkey-patch leve em `history.pushState/replaceState` + listener `popstate`) suportam SPA navigation de `/account` → `/trade` sem F5. Restringir `@match` para `/trade*` **quebra** esse fluxo porque Tampermonkey não re-injeta em pushState. Solução: match largo + guard `CHART_PATH_REGEX` no startScan para não desperdiçar CPU fora de `/trade`.
- 2026-05-18 — TopstepX usa **TradingView Advanced Charts (charting_library) versão `TT v31.1.0`**, não TradingView.com. Não tem Pine Script Editor; indicadores customizados precisam ser em **JavaScript** seguindo o formato `metainfo + constructor` da API. A brecha para injetar é interceptar `window['tradingview_XXXX'].getCustomIndicators` (sufixo dinâmico — precisa scan).
- 2026-05-18 — A API custom indicators **não expõe** `request.security()` (MTF) nem desenho de `table` (dashboard). Também não dispara alertas configuráveis via `alertcondition`. No port do indicador MNQ, esses três blocos do Pine foram cortados do MVP.
- 2026-05-18 — `_metainfoVersion: 51` aceita `plot.type: 'shapes'` com `plottype` em `shape_diamond`, `shape_triangle_up/down`, `shape_xcross`, etc. — vale para reproduzir os `plotshape` do Pine.
- 2026-05-18 — `PineJS.Std.vwap(ctx)` pode não estar exposto em todas as versões da charting_library. O userscript faz fallback para VWAP manual com reset por dia UTC (não é a sessão CME ideal, mas serve para MVP).
- 2026-05-18 — O modal vermelho **"Indicator error — Oops. Something has gone wrong with one or more of your indicators"** no TopstepX pode ser estado transitório do widget (lifecycle do service-worker / unload listener deprecated). **F5 resolve** quando não há erro real no código do userscript. Antes de mexer no script, sempre conferir o console: se aparecer `[Pullback MNQ] custom indicator injetado` + `N custom indicators loaded` + `getBars()` retornando dados, o nosso indicador está OK.
- 2026-05-18 — Em chart de **tick (100T, 500T)**, o console fica poluído com `ChartApi.PointsetsManager:Cannot get index of time: time=…`. **Não é do nosso indicador** — é o `Topstep Daily Levels` (Custom indicator nativo da Topstep) tentando plotar timestamps fixos (abertura/fechamento RTH, em UTC) que não correspondem a nenhuma barra da timescale de tick (barras de tick não têm hora fixa). Para silenciar: desativar o Daily Levels no chart de tick, ou usar chart por tempo (1m/5m). Não afeta funcionamento.
- 2026-05-18 — Diagnóstico de erro real do indicador: tudo do `Pullback MNQ` é logado com prefixo `[Pullback MNQ]` (LOG/WARN). Erros internos do charting_library aparecem em `library.<hash>.js` — só são problema nosso se a stack trace mencionar `pullbacks_mnq_bone_zone` ou `tv-basicstudies-1`.

## Histórico de tarefas concluídas

- 2026-05-22 — Tests p/ `auth.py` (25 testes em 10 classes — current_user/_id, _read_secret, _apply_session_to_client, _session_to_dict, _redirect_url, _sign_in/_up_email, get_client, _t). Padrão `FakeSessionState` mockando st.session_state. Suite total 395 verdes (era 370).
- 2026-05-22 — Tests p/ `metrics.compute_coach` + 8 helpers (25 testes em 9 classes). Fecha cobertura completa das 8 funções públicas de `metrics.py`. Suite total 370 verdes (era 345).
- 2026-05-22 — Tests p/ `metrics.compute_overview` (22 testes em 7 classes — empty/counts/pnl/duration/daily/direction/best_worst_trade). Cobre 22 chaves do dict de KPIs USD, divisão-por-zero em best_day_pct, NaN-robust em total_lots. Documenta que `avg_losing_trade` preserva sinal (diferente de compute_kpis). Suite total 345 verdes (era 323).
- 2026-05-22 — Tests p/ `metrics.compute_duration_buckets` (13 testes em 5 classes — empty/shape/classification/win_rate/types). Cobre 11 buckets `[lo, hi)`, edge case boundary inclusiva/exclusiva, open-ended "4 hours and up". Suite total 323 verdes (era 310).
- 2026-05-22 — Tests p/ `metrics.compute_daily` (13 testes em 5 classes — empty/single_day/multiple_days/reward_risk/day_col). Cobre groupby + sort + dual-tz preference (trade_day_et vs trade_day). Suite total 310 verdes (era 297).
- 2026-05-22 — Tests p/ `metrics.compute_segments` (13 testes em 5 classes — empty/partitioning/aggregation/win_rate/schema). Cobre 4 buckets de operações por adições, incluindo edge case Flat com adição. Suite total 297 verdes (era 284).
- 2026-05-22 — Tests p/ `metrics.compute_kpis` (22 testes em 6 classes — empty/counts/sums/averages/rr_ratio/grouped/schema). Cobre 13 KPIs em pontos + divisão-por-zero protegida. Suite total 284 verdes (era 262).
- 2026-05-22 — Tests p/ `metrics.compute_groups` (15 testes em 6 classes — empty/single/overlap/isolation/ordering/status/duration). Cobre motor de agrupamento que é base de KPIs/segmentos/aderência. Suite total 262 verdes (era 247).
- 2026-05-20 — **Loop autônomo de backlog (pós security review)**: 5 itens entregues sequencialmente.
  (1) **M-3 fix:** `PRD/m11_live_snapshots_unique.sql` adiciona `UNIQUE (user_id, account_id, snapshot_at)`; Edge Function `live-ingest` migrou de `.insert` para `.upsert({ onConflict, ignoreDuplicates: true })`. Retries da extensão agora são idempotentes. `m6_README.md` atualizado com a ordem M9/M10/M11.
  (2) **M-2 bloqueado e documentado:** download de bundle JS externo (`esm.sh/@supabase/supabase-js`) para `assets/vendor/` foi bloqueado pelo classificador (supply chain não autorizado pelo agente). Criado `assets/vendor/README.md` com o procedimento manual para o operador rodar quando quiser.
  (3) **Tests unitários:** pasta `tests/` nova com 28 testes em `unittest` stdlib (zero dependência nova). Cobertura: `metrics.compute_plan_adherence` (9 cenários — todas as 5 categorias + corner cases), `daily_plan.compute_usd` + `point_value_usd` (multi-contrato + edge cases), `timezones` (PRIMARY_TZ, `_tz_short`, `user_tz`, `to_primary`, `to_user`, `fmt_dual` com fuso igual ao primário). Roda com `.venv/Scripts/python.exe -m unittest discover -s tests` em ~0.1s. `tests/__init__.py` silencia loggers do streamlit (parcial).
  (4) **i18n da extensão Chrome:** `extension/_locales/{en,pt_BR,es}/messages.json` com 19 chaves cada. `manifest.json` agora usa `__MSG_extName__` + `__MSG_extDescription__`. `popup.html` ganhou atributos `data-i18n` / `data-i18n-placeholder`; `popup.js` aplica via `chrome.i18n.getMessage` (substring `M(key, [args])` helper). `package_extension.py` agora inclui `_locales/**/*` — zip cresceu de 11 para 14 arquivos.
  (5) **README.md da raiz reescrito:** seção do Live Monitor + extensão + Edge Functions + tests + estrutura atualizada + estado da fusão. Aponta para `guia_execucao.md` e `ENCERRAMENTO_trade_agent.md`.
- 2026-05-20 — **Security review da fusão (pós Fase 4)**: revisão manual cobrindo Edge Function `live-ingest`, trigger `risk_guard_eval`, RLS das 7 tabelas novas, JWT handling no Streamlit + extensão Chrome, Web Notifications. **3 fixes aplicados**: (M-1) `_inject_web_notifications` em [src/live.py](src/live.py) trocou interpolação f-string por `json.dumps(...).replace("</", "<\\/")` — padrão SSR React/Next.js que neutraliza `</script>` em qualquer campo, mesmo vindo de fonte confiável; (M-4) comentário em `purge_old_live_snapshots()` alertando que apaga para TODOS os usuários numa transação só, evitar rodar manualmente em horário operacional; (B-3) warning em `extension/config.js` proibindo colar SERVICE_ROLE_KEY (apenas ANON_KEY). **3 itens em backlog** (não bloqueiam deploy mas vão para futuro): M-2 substituir dependência runtime de `esm.sh` por bundle no Supabase Storage; M-3 idempotência UNIQUE + rate limit na Edge Function `live-ingest`; B-2 validar assinatura do JWT antes do `atob` no popup (hoje só display). Smoke test: `json.dumps({"jwt":"evil</script><h1>x"}).replace("</", "<\\\\/")` produz `evil<\\/script>` — parser HTML não enxerga como fim de tag. **Risco residual: BAIXO.** Recomendação para deploy: prosseguir.
- 2026-05-20 — **Fase 4 da fusão — Cleanup**: gerado `PRD/ENCERRAMENTO_trade_agent.md` (post-mortem de 12 seções seguindo o template do CLAUDE.md global) cobrindo dor/problema, resultado, esforço intelectual (pesquisa, planejamento, feature engineering, IA estratégica, trade-offs, hipóteses validadas/refutadas), áreas beneficiadas, tecnologias (FastAPI + SQLite + Next.js + Chrome MV3 + Claude Agent SDK), decisões-chave (7 entradas), histórico Git com top-10 commits estratégicos, ~63h estimadas. Trade_Agent ainda **não foi tagueado** como `v-archived` — bloqueado pelo classificador de segurança (mexer em git de outro repo não autorizado explicitamente). Os passos manuais (copy ENCERRAMENTO.md → raiz do Trade_Agent + warning no README + tag `v-archived`) estão documentados na seção 12 do próprio arquivo. **Status da fusão:** todas as 4 fases codadas. Falta validar end-to-end (extensão na produção, snapshot real, alerta dispara) e arquivar Trade_Agent manualmente.
- 2026-05-20 — **Fase 3 da fusão — Real-time + Risk Guard + Web Notifications + CRUD alertas**: 6 entregas.
  (i) `src/risk_settings.py` novo: `get_settings()`, `upsert_settings(payload)`, `apply_account_defaults(account_type)` com 4 perfis (Express 50K/100K/150K/Custom). Reusa `auth.get_client` e `coach_ai._current_user_id`.
  (ii) `src/settings.py` substituiu o stub de Risk Guard por CRUD completo: select de account_type com defaults sugeridos quando troca, 4 number_inputs (DLL, Trailing DD, Max Size, Warning %), botão Save → upsert.
  (iii) `PRD/m10_risk_guard_trigger.sql`: trigger `AFTER INSERT` em `live_snapshots` chama `public.risk_guard_eval()` (security definer) que avalia as 4 regras MVP (DLL, Trailing DD, Max Size, Unplanned Addition) consultando `risk_settings` + `daily_plans` (na sessão NY/ET). Cada regra cria linha em `alerts` via helper privado `_rg_insert_alert` que aplica **cooldown de 5min por (user_id, alert_type)** para evitar spam. Idempotente.
  (iv) `src/alerts.py` novo: `list_recent(limit)`, `mark_read(id)`, `mark_dismissed(id)`, `mark_all_read()`. RLS isola por user_id.
  (v) `src/live.py` reescreveu `_section_alerts`: ação bar com contador de não-lidos + botão "Marcar todos lidos", lista compacta com cartão por alerta mostrando severity colorida + dual-tz no timestamp + botões inline "Marcar lido" e "Descartar" (visíveis apenas se ainda não usados). Novo `_inject_web_notifications(user_id)` injeta componente HTML/JS leve com supabase-js do esm.sh: pede permissão de notificação no primeiro load, assina canal `bi-alerts-<user_id>` filtrado por `user_id=eq.<id>`, dispara `Notification` no INSERT de severity warn/critical (`requireInteraction` em critical). Componente carrega 1x por render da aba.
  (vi) 21 chaves i18n novas nos 3 idiomas (live.alerts.* + settings.risk.*).
  **Pendências manuais:** aplicar `PRD/m10_risk_guard_trigger.sql` no Supabase + redeploy não precisa (somente SQL). Validar fluxo end-to-end: configurar risk_settings → extensão envia snapshot com `day_pnl = -DLL` → linha aparece em `alerts` → Web Notification dispara no browser do trader.
- 2026-05-20 — **Fase 2 da fusão — extensão Chrome MV3 multi-tenant + Edge Function `live-ingest`**: 8 entregas.
  (i) Pasta `extension/` migrada de `E:\BD\Trade_Agent\chrome_extension\` e reescrita: `manifest.json` v0.1.0 (nome "BI TopStep — Live Monitor", permissions enxutas, content scripts em `https://topstepx.com/*`, host_permissions para `*.supabase.co/*`), `config.js` novo com placeholders `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`LIVE_INGEST_URL`, `selectors.json` rebatizado para `/trade` (9 campos: account_id, position_size/avg_price/contract/side, unrealized/realized/day_pnl, drawdown) com fallback regex e flag de validação manual.
  (ii) `background.js` reescrito do zero: importScripts(config.js), `chrome.alarms` 30s para heartbeat + debounce de 5s para eventos, `sendSnapshot()` via fetch com Authorization Bearer JWT + apikey ANON, persiste `last_status`/`scrape_status` em chrome.storage.local.
  (iii) `content.js` reescrito: poll de 5s, scrape via `selectors.json` + fallback regex, detecção de meaningful change para evitar spam, marca `scrape_status=broken` após 3 ciclos vazios consecutivos.
  (iv) `popup.html/js/css` enxutos: campo textarea para colar JWT, botões Save/Test, decode do payload do JWT para mostrar email + exp, indicadores coloridos para config/scrape/last_status.
  (v) Edge Function `supabase/functions/live-ingest/index.ts`: Deno + supabase-js, valida JWT via `supabase.auth.getUser`, modo ping (`{ping:true}`) + modo INSERT em `live_snapshots` com `user_id` sempre derivado do token (nunca do payload), CORS para chrome-extension://. README completo + `.env.example`.
  (vi) `src/account.py` ganha seção "Live Monitor extension" visível só para `billing.has_feature("live_monitor")`: text_input com o JWT da sessão em modo password (Streamlit dá botão "ver/copiar" nativo).
  (vii) Nova aba `src/live.py` (visível só para Pro/Trial): 5 sub-seções (status conexão com idade do último snapshot, posição atual, PnL realized/day/drawdown, alertas recentes, instalação). Polling via `streamlit-autorefresh` (3s) com graceful degradation para botão manual se a lib não estiver instalada. `streamlit-autorefresh>=1.0` adicionado ao `requirements.txt`.
  (viii) `scripts/package_extension.py` empacota a pasta `extension/` em `dist/extension/extension-latest.zip` + `extension-<version>.zip`; flags `--supabase-url`/`--anon-key` permitem injetar config no `.zip` em build time sem modificar o arquivo original. Smoke test gerou zip com 11 arquivos (sem offscreen/sounds/tests/styles do Trade_Agent legado). `dist/` adicionado ao `.gitignore`.
  Migration extra: `PRD/m9_features.sql` aplica `features.live_monitor=true` em `plans.slug in ('pro','admin','trial')` e remove de basic/free. 32 chaves i18n novas nos 3 idiomas (tab.live + live.* + account.extension_*). Próximo: aplicar `m9_features.sql` no Supabase, fazer deploy da Edge Function, validar end-to-end com a extensão no browser e iniciar Fase 3 (Realtime + Risk Guard trigger + Web Notifications).
- 2026-05-20 — **Fase 1.1 da fusão — adições não-planejadas refinadas**: `compute_plan_adherence` passou de 3 para 5 categorias. Novas: (a) **`against_plan`** — plano existe para o contrato em direção oposta (trader operou contra a tese matinal); toma precedência sobre `unplanned` quando há plano cruzado. (b) **`size_creep_day`** — soma rolante de `total_size` por (day, contract, direction) ultrapassa o `max_size` do plano ao longo do dia, mesmo quando cada operação isolada cabia no limite (detecta tilt distribuído em múltiplos grupos). `trade_day` agora deriva via `America/New_York` (alinhado com `trade_day_et` do M7). Tupla `VIOLATION_TYPES` exportada para uso na UI. Smoke test sintético com 5 grupos: cada categoria contabilizada corretamente, score 20% (1/5 compliant). UI do Dashboard atualizada com 6 contadores no card de score + dois novos labels na tabela de violações. 4 chaves i18n novas nos 3 idiomas. Explore do `processor.py` Legacy confirmou que ele não tinha regras adicionais além das 3 originais — as duas novas foram implementadas com schema atual (sem migration). Próximo: Fase 2 (extensão Chrome + Edge Function `live-ingest`).
- 2026-05-20 — **Fase 1.2 da fusão — dual-timezone NY/ET + aba Configurações**: 6 entregas. (i) `src/timezones.py` novo com `PRIMARY_TZ=America/New_York`, `user_tz()` resolvendo de session/user_metadata/fallback e `fmt_dual(ts)` formatando "HH:MM ET / HH:MM <user_tz_short>". (ii) `src/settings.py` novo com 4 sub-seções (idioma reusa `i18n.language_selector`, fuso com auto-detect via JS injetado por `st.components.v1.html` e persistência em `user_metadata.preferred_tz`, Risk Guard stub atrás de `billing.has_feature("live_monitor")`, contas stub). (iii) Aba "Configurações" entre Importar e Account em `src/app.py`. (iv) `load_trades` deriva `trade_day_et` (entered_at em ET) e `entry_hour` agora usa `timezones.user_tz()` em vez de hardcoded BRT. (v) `metrics.compute_daily`, `compute_overview` e `_coach_overtrading` usam `_day_col(df)` que prefere `trade_day_et` quando presente — coluna renomeada para `trade_day` no output (back-compat com charts). (vi) Cards Best/Worst Trade e tabela de trades usam `fmt_dual`; nova coluna `entered_at_dual` na tabela. (vii) 21 chaves i18n novas (tab.settings, settings.*, dash.trades.col.entered_at) replicadas em EN/PT-BR/ES. Smoke test offline: `fmt_dual('2026-05-20 14:32 UTC') == '20/05 10:32 ET / 20/05 11:32 BRT'`. Próximo: Fase 1.1 (refinar adições não-planejadas).
- 2026-05-20 — **Fase 1.3 da fusão — schemas Postgres novos**: 7 migrations idempotentes em `PRD/m6_*.sql` (contracts, live_snapshots, alerts + enums, tilt_patterns, payouts, risk_settings, accounts) prontas para aplicar manualmente no Supabase SQL Editor. Ordem documentada em `PRD/m6_README.md`. `live_snapshots` tem job de purge 7 dias via `pg_cron` (instrução comentada no SQL — rodar uma vez após aplicar). `src/daily_plan.py` adaptado: novo `_load_contract_values()` lê tabela `public.contracts` com cache em memória, fallback estático (`{"MNQ": 2.0}`) se a tabela ainda não foi aplicada — zero quebra em ambiente pré-M6. Smoke test offline: `point_value_usd("MNQ") == 2.0`, `point_value_usd("XYZ") is None`. T0.1 já estava feito (tag `v-final-pre-merge` em `45d6f91` do Trade_Agent).
- 2026-05-20 — **Rodada de definições + `guia_execucao.md`**: 7 rodadas de perguntas estruturadas com o usuário consolidaram **24 decisões** sobre a fusão Trade_Agent → BI TopStep (ordem de fases, auth da extensão, pricing live, regras Risk Guard MVP, schemas de `live_snapshots`/`alerts`/`tilt_patterns`/`payouts`/`risk_settings`/`accounts`/`contracts`, dual-timezone NY/ET + auto-detect, tela Configurações, base da extensão, frequência snapshot, distribuição dev-only, realtime via `st_autorefresh` + Web Notifications, trigger Postgres para Risk Guard, retenção 7d). Gerado `guia_execucao.md` na raiz com 4 fases (1.3 → 1.2 → 1.1 → 2 → 3 → 4) em 30+ tarefas auto-contidas (T0 a T4.4). Plano em `~/.claude/plans/favor-ler-o-todos-purring-cupcake.md`. Decisões agregadas em [`DECISOES.md`](DECISOES.md).
- 2026-05-20 — **M4 Stripe webhook mergeado em `saas-main`** (merge commit `6df8737`, parents `9fd0c26 4978379`). Edge Function `supabase/functions/stripe-webhook/index.ts` trata 6 eventos críticos com upsert idempotente, validação HMAC e mapeamentos `price_id → plan_slug` + status Stripe → enum. Adicionados `README.md` e `.env.example` na pasta da função documentando deploy via supabase CLI, secrets esperados e teste local via Stripe CLI. Backlog explícito: idempotência por `event.id`, eventos adicionais (refunds/disputes), logging JSON, testes automatizados. Próximo: deploy em Supabase + cadastro do endpoint no Stripe Dashboard. Desbloqueia Fase 1 da fusão.
- 2026-05-20 — **Comparativo tático Trade_Agent ↔ BI TopStep + decisão de fusão**: gerado panorama lado-a-lado dos dois projetos (modelo, stack, estado, sobreposições, complementaridades). Direção escolhida: BI TopStep absorve Trade_Agent. Real-time entra como MVP via Supabase Realtime. Coach segue copy-paste. Trade_Agent arquivado pós-migração. Roadmap em 4 fases definido. Decisão registrada em `DECISOES.md`. Plano completo em `~/.claude/plans/me-fa-a-um-comparativo-soft-bear.md`.
- 2026-05-20 — **Refatoração de layout**: movidos `app.py`, `auth.py`, `account.py`, `action_plan.py`, `billing.py`, `coach_ai.py`, `i18n.py`, `ingest_core.py`, `ingest.py` e `metrics.py` da raiz para `src/`. Ajustados 4 paths baseados em `__file__`, 3 `.bat`, `CLAUDE.md`, `PRD/README.md`. Criado `README.md` na raiz. `login-screen.png` movido para `assets/`. `__pycache__/` órfão removido.
- 2026-05-20 — **v0.3.0** do userscript Pullbacks MNQ: refactor amplo em 4 blocos (estrutura, bootstrap, performance, sanidade). Hot path otimizado (cache de boundary VWAP CME, drop `new_var` ociosos, consolidação de cálculos). Bootstrap robusto (detecção por shape + soft-watch + SPA navigation). Sanidade de runtime (clamp de inputs com WARN single-shot, feature-detect `PineJS.Std.*`, reset em troca de símbolo/TF, TTL configurável de linhas, AudioContext singleton). Novo input `linhasTTLBars`. Criado `PRD/CHANGELOG_pullback_mnq.md` e `DECISOES.md` (decisão sobre single-file + CHANGELOG vs build tooling).
- 2026-05-18 — Port v0.1.0 do indicador "Pullbacks MNQ - Bone Zone v7.1" para userscript JS no formato da charting_library da TopstepX. Sem MTF, sem dashboard, sem alertas (limitações da API).
- 2026-05-18 — Diagnóstico do erro "Indicator error" em produção: confirmado como transitório (F5 resolve). Identificado ruído de `PointsetsManager` em tick charts como vindo do `Topstep Daily Levels`, não do nosso script.
- 2026-05-18 — v0.2.0 do userscript Pullbacks MNQ: VWAP com reset por sessão CME (18:00 ET via `Intl.DateTimeFormat` com timezone NY), 5 plots de linha persistentes (Entry/Stop/Tgt2/Tgt4/Tgt6) que perduram até próximo sinal, `console.info('[Pullback MNQ] SIGNAL', {...})` estruturado + beep Web Audio API (880 Hz long, 440 Hz short) — debounce por timestamp da barra + janela de 5min para não spammar no replay histórico. Inputs novos: `mostrarLinhas` (bool, default true), `tocarSom` (bool, default true).
