{
  "name": "chrome-extension-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Regression tests para chrome_extension (Story 1-5). npm test = Vitest sobre logica de extractor + manifest. npm run test:e2e = Playwright headed (manual, exige Chrome real e sessao logada).",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "happy-dom": "^15.7.0",
    "vitest": "^2.1.0"
  }
}
